# frontend

Reserved for the React + Vite streaming chat SPA (per `docs/ROADMAP.md`). Not yet scaffolded —
this directory is not a `uv` workspace member; it will get its own `package.json` when frontend work
starts.
