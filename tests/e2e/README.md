# tests/e2e

Reserved for full-stack end-to-end tests (frontend + chat + scheduler together). Not yet
populated — `services/frontend` itself isn't scaffolded yet. Run via `make test-e2e` once tests
exist; until then this target exits with pytest's "no tests ran" status, which is expected.

See `docs/testing-strategy.md` for the full testing convention.
